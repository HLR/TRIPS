#!/bin/bash
echo $PWD
for file in $PWD/Data/SpRLTrainTRIPS_parses/*.xml; do
	[ -e "$file" ] || continue
	xsltproc "$PWD/transform_changeOffset.xsl" "${file}" > "${file}.clean"
	xmllint -format "${file}.clean"
	diff "${file}" "${file}.clean" > "${file}.log"
done
